@model TiendaMagic.Models.Query

@{
    ViewData["Title"] = "Details";
}

    <h1>@Model.User.Nick</h1>

<div>
    <h4>@Model.Date</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Nombre
        </dt>
        <dd class="col-sm-10">
            @Model.User.Name @Model.User.LastName
        </dd>
        <dt class="col-sm-2">
            Consulta
        </dt>
        <dd class="col-sm-10">
            @Model.Text
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Resuelta</a> |
    <a asp-action="Index">Volver</a>
</div>
